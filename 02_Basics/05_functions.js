
const greet = (name)=>{
    console.log(`Hii ${name}`)
}

greet("Prabal")

const addTwo= (num1,num2) => {
    return num1+num2
}
console.log(addTwo(2,7));

const add= (num1,num2) =>  (num1+num2) //implicit return

//if using curly braces we would need to use return else we can directly use the above syntax

